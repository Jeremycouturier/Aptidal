In this folder, we show some examples of use of Aptidal. In order to run an example, you need to:
- set the files parameters.h and main.c in the /src folder as in the example
- set the input/output path in the parameters.h file with a path that exists on your architecture
- Run aptidal with the command ./aptidal.sh from within the /src directory.
